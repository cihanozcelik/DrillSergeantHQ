# 🎾 PaddleBall Q-learning (WASM + wgpu)
## Implementation guide index

This experiment is a **learning-focused** RL project built on top of the existing WebGPU rendering scaffold.

**Important**: this experiment is **not deep learning** (no neural network). It uses **tabular Q-learning** (a Q-table).

### Where to start

- **Plan (what to build, with todo IDs)**: `plan.md`
- **Implementation guide (how to build it, step-by-step)**: `guide/README.md`


