{
  "name": "paddleball-qlearn-wasm-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:wasm": "cd ../rust && wasm-pack build --dev --target web --out-dir ../web/src/pkg",
    "dev:wasm:watch": "concurrently -k -n VITE,WASM -c cyan,magenta \"vite\" \"npm run dev:wasm && cargo watch -w ../rust -s 'wasm-pack build --dev --target web --out-dir ../web/src/pkg'\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.0"
  }
}


